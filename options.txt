# Optimized settings for low-end hardware (Win2000, 512MB RAM, 16MB GPU)
# View distance - lower = much faster (default 512)
viewdist=48

# Render type - "fast" disables clouds, fog, overhead sky
normal=fast

# Disable smooth lighting for faster chunk building
gfx-smoothlighting=false

# Disable mipmaps to save VRAM
gfx-mipmaps=false

# Reduce chunk updates per frame (default 30)
gfx-maxchunkupdates=8

# Use simple fog (sky color background instead of GPU fog)
gfx-simplefog=true

# Disable clouds
gfx-clouds=false

# Disable entity shadows
entityshadow=None

# Disable player names for less rendering
namesmode=None

# Limit FPS to reduce CPU/GPU load
fpslimit=Limit120FPS
